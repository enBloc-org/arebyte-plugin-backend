meta {
  name: deleteSingleProject
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/projects/1
  body: none
  auth: none
}

tests {
  test("should be forbidden to public users", function(){
    const data = res.getBody()
    expect(res.getStatus()).to.equal(403)
  })
}
